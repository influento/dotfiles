# Global npm packages to install and keep updated (all profiles)
# One package per line (npm package name, as used with `npm install -g`)
#
# Note: @anthropic-ai/claude-code is managed by Anthropic's native installer
# (~/.local/share/claude/versions/), not npm — do not add it here.
