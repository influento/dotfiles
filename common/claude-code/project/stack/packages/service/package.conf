# A backend service: the runtime and the database. Tracing, logs and metrics
# are in effect itself (effect/unstable/observability, see its rule).
KIND=preset
NEEDS="effect drizzle"
NOTE="Preset: effect, drizzle."
