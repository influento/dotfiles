# shardx-scripts — anti-detect browser fleet on ShardX, run as scripts.
# Private repository; the host comes from git config private.root, never from here.
KIND=toolkit
REFERENCE=private:shardx-scripts
REF=main
DEP=
SKILLS_FROM_REFERENCE=.claude/skills
NOTE="Anti-detect browser fleet on ShardX: one account = one profile, proxy, machine and window. Entry points settle.mjs and launch/run.mjs."
