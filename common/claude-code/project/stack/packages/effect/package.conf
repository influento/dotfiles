# Effect v4 — the runtime every TypeScript project here is written in: typed
# errors, DI, Schema, Schedule, Stream, HTTP, RPC, CLI. In every TypeScript
# project by project-setup; the other packages NEEDS it.
# One version for effect and every @effect/*: bump REF and both DEP lines together.
KIND=lib
REFERENCE=git:https://github.com/Effect-TS/effect.git
REF=effect@4.0.0-rc.115
DEP="effect@4.0.0-rc.115 @effect/platform-node@4.0.0-rc.115"
DEV_DEP="@effect/vitest@4.0.0-rc.115"
NOTE="Effect v4, the runtime: typed errors, Layer, Schema, Schedule, Stream, effect/unstable/{http,httpapi,rpc,cli}. Read repos/effect/LLMS.md before writing any of it."
