# Drizzle ORM on Effect, libSQL — drizzle-orm/effect-libsql over
# @effect/sql-libsql (@libsql/client underneath: a local file with a file:
# URL, or Turso over libsql:// with an auth token). One package per dialect
# (drizzle-postgres, drizzle-sqlite, drizzle-mysql beside this one): the
# same Db shape, the driver and the table module differ. Same drizzle-orm
# hash-build pin as drizzle-postgres, for the same reason (see there).
# Verified 2026-09-12 on a file URL: generate, migrate, insert returning,
# select. Turso itself not exercised.
KIND=lib
NEEDS=effect
DEP="drizzle-orm@1.0.0-rc.5-5935859 @effect/sql-libsql@4.0.0-rc.115"
DEV_DEP="drizzle-kit@1.0.0-rc.4"
NOTE="Drizzle ORM through drizzle-orm/effect-libsql over @effect/sql-libsql (file or Turso): Db service in src/db/db.ts, tables in src/db/schema.ts, migrations generated by drizzle-kit and applied by src/db/migrate.ts."
