# A full-stack app: a service plus TanStack Start, atom-react, shadcn.
# Auth: none in the registry (Better Auth dropped 2026-09-12, no Effect API planned); written per project when needed.
KIND=preset
NEEDS="service tanstack-start atom-react shadcn"
NOTE="Preset: service (effect, drizzle) + tanstack-start, atom-react, shadcn."
