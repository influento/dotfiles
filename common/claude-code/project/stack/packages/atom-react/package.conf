# @effect/atom-react — Effect's own React state (Tim Smart's effect-atom, in
# the monorepo since v4). The client's only reactive system: no TanStack Query,
# no Zustand, no Redux.
KIND=lib
NEEDS=effect
DEP="@effect/atom-react@4.0.0-rc.115"
NOTE="Client state with @effect/atom-react: an Atom per piece of state, AtomRpc for the RPC client; components read a Result, never run an Effect."
