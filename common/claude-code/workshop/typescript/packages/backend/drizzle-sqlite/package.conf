# Drizzle ORM on Effect, SQLite — drizzle-orm/effect-sqlite-node over
# @effect/sql-sqlite-node, which speaks node:sqlite (Node 22.5+; no native
# module, no server, a file per environment). One package per dialect
# (drizzle-postgres, drizzle-mysql, drizzle-libsql beside this one): the
# same Db shape, the driver and the table module differ. Same drizzle-orm
# hash-build pin as drizzle-postgres, for the same reason (see there).
# Verified 2026-09-12 on Node 26: generate, migrate, insert returning, select.
KIND=lib
NEEDS=effect
DEP="drizzle-orm@1.0.0-rc.5-5935859 @effect/sql-sqlite-node@4.0.0-rc.115"
DEV_DEP="drizzle-kit@1.0.0-rc.4"
NOTE="Drizzle ORM through drizzle-orm/effect-sqlite-node over @effect/sql-sqlite-node (node:sqlite): Db service in src/db/db.ts, tables in src/db/schema.ts, migrations generated by drizzle-kit and applied by src/db/migrate.ts."
