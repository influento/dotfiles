# Drizzle ORM on Effect, MySQL — drizzle-orm/effect-mysql2 over
# @effect/sql-mysql2 (mysql2 underneath). One package per dialect
# (drizzle-postgres, drizzle-sqlite, drizzle-libsql beside this one): the
# same Db shape, the driver and the table module differ. Same drizzle-orm
# hash-build pin as drizzle-postgres, for the same reason (see there).
# Verified 2026-09-12 against MySQL 8: generate, migrate, insert
# $returningId, select.
KIND=lib
NEEDS=effect
DEP="drizzle-orm@1.0.0-rc.5-5935859 @effect/sql-mysql2@4.0.0-rc.115"
DEV_DEP="drizzle-kit@1.0.0-rc.4"
NOTE="Drizzle ORM through drizzle-orm/effect-mysql2 over @effect/sql-mysql2: Db service in src/db/db.ts, tables in src/db/schema.ts, migrations generated by drizzle-kit and applied by src/db/migrate.ts."
