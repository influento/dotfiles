# mpv keybindings

# Volume (scroll wheel + keys)
WHEEL_UP add volume 2
WHEEL_DOWN add volume -2
UP add volume 5
DOWN add volume -5

# Seeking (left/right arrows)
LEFT seek -5
RIGHT seek 5
Shift+LEFT seek -1 exact
Shift+RIGHT seek 1 exact

# Toggle loop
l cycle-values loop-file "inf" "no"
