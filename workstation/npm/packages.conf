# Global npm packages to install and keep updated (workstation only)
# One package per line (npm package name, as used with `npm install -g`)
# Packages needed on all profiles belong in common/npm/packages.conf
@mermaid-js/mermaid-cli
